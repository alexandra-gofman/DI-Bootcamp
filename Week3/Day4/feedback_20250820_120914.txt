URL: https://github.com/alexandra-gofman/DI-Bootcamp/blob/main/Week3/Day4/Daily_Challenge_Circle.py
suggestions for improvement:
- Consider adding `@property` decorators for `radius` and `diameter` with corresponding setters to ensure that when one attribute (e.g., `radius`) is modified directly, the other (e.g., `diameter`) automatically updates to maintain consistency. For example:

```python
class Circle:
    def __init__(self, radius=None, diameter=None):
        # ... (existing init logic)

    @property
    def radius(self):
        return self._radius

    @radius.setter
    def radius(self, value):
        if value < 0: raise ValueError("Radius cannot be negative")
        self._radius = value
        self._diameter = 2 * value

    @property
    def diameter(self):
        return self._diameter

    @diameter.setter
    def diameter(self, value):
        if value < 0: raise ValueError("Diameter cannot be negative")
        self._diameter = value
        self._radius = value / 2
```
- Implement a `__repr__` dunder method in addition to `__str__`. While `__str__` is for user-friendly output, `__repr__` provides an unambiguous, developer-focused string representation that ideally could be used to recreate the object (e.g., `Circle(radius=30)`).
- Add docstrings to the `Circle` class and its methods to explain their purpose, arguments, and return values. This significantly improves code readability and maintainability for future developers (and your future self).
- While not strictly necessary given the current implementation, for classes that require many rich comparison methods (`__lt__`, `__le__`, `__gt__`, `__ge__`), consider using `@functools.total_ordering` from the `functools` module. This decorator simplifies the process by only requiring `__eq__` and one of the other comparison methods (e.g., `__lt__`), and it automatically fills in the rest.
Brief justification:
- correctness: The code accurately and completely addresses all requirements specified in the 'Chapter Content/Questions'. It correctly initializes a `Circle` by radius or diameter, allows querying both, computes the area, uses `__str__` for printing attributes, implements `__add__` for adding circles (summing radii), and defines `__gt__`, `__lt__`, and `__eq__` for comparison, enabling list sorting. The bonus task of drawing sorted circles using the Turtle module is also correctly implemented.
- readability: The code is well-structured and easy to understand. Variable and method names are clear, and the use of f-strings in `__str__` is good practice. The logic within methods is straightforward. A minor area for improvement is the absence of docstrings, which would further enhance readability for larger or more complex classes.
- performance: For the functionality described, the code exhibits optimal performance. The operations involved are basic arithmetic calculations and object instantiations, which are inherently fast. There are no complex algorithms or data structures that would introduce performance bottlenecks. The Turtle module's drawing speed is inherent to the library and not a performance issue with the `Circle` class itself.
- security: The code does not handle any external inputs, interact with files, or perform network operations. It exclusively processes internal numerical data. There are no known security vulnerabilities such as injection flaws or insecure data handling practices. The code is self-contained and operates within a safe computational environment.

