URL: https://github.com/alexandra-gofman/DI-Bootcamp/blob/main/Week1/Day6/Exercises_XP.py
suggestions for improvement:
- In Exercise 2, the code doesn't handle potential errors if the user inputs non-numeric values for ages.  Add error handling (e.g., `try-except` blocks) to gracefully handle such situations.
- In Exercise 3, the `major_color` dictionary's value for 'US' is a list, while others are strings.  This inconsistency might make it harder to work with. Standardize the data types (e.g., always use lists).
- In Exercise 3, instead of directly manipulating the `brand` dictionary, consider creating a function to improve code organization and reusability.
- The code for Exercise 4 is excellent and directly addresses the problem.
Brief justification:
- correctness: The code accurately completes exercises 1, 2, 3, and 4, aligning with the chapter's content on dictionaries, loops, conditionals, and nested data structures.  Minor issues with error handling and data type consistency slightly reduce the score.
- readability: The code is generally well-structured and easy to follow, with clear variable names and comments.  However, some improvements could make it even more readable (e.g., error handling, data consistency).
- performance: The code's performance is efficient for the tasks involved, with no obvious bottlenecks. The use of `zip` and `enumerate` in certain places is especially efficient.
- security: The code does not present any security vulnerabilities as it does not interact with external systems or handle sensitive data.

